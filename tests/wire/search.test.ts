// This file was auto-generated by Fern from our API Definition.

import * as Forum from "../../src/api/index";
import { ForumClient } from "../../src/Client";
import { mockServerPool } from "../mock-server/MockServerPool";

describe("SearchClient", () => {
    test("search (1)", async () => {
        const server = mockServerPool.createServer();
        const client = new ForumClient({
            maxRetries: 0,
            apiKey: "test",
            provisioningKey: "test",
            environment: server.baseUrl,
        });

        const rawResponseBody = {
            data: { items: [{ type: "thread", id: "id" }], nextCursor: "nextCursor", hasMore: true },
        };
        server.mockEndpoint().get("/search/").respondWith().statusCode(200).jsonBody(rawResponseBody).build();

        const response = await client.search.search();
        expect(response).toEqual({
            data: {
                items: [
                    {
                        type: "thread",
                        id: "id",
                    },
                ],
                nextCursor: "nextCursor",
                hasMore: true,
            },
        });
    });

    test("search (2)", async () => {
        const server = mockServerPool.createServer();
        const client = new ForumClient({
            maxRetries: 0,
            apiKey: "test",
            provisioningKey: "test",
            environment: server.baseUrl,
        });

        const rawResponseBody = { error: { code: "code", message: "message" } };
        server.mockEndpoint().get("/search/").respondWith().statusCode(400).jsonBody(rawResponseBody).build();

        await expect(async () => {
            return await client.search.search();
        }).rejects.toThrow(Forum.BadRequestError);
    });

    test("search (3)", async () => {
        const server = mockServerPool.createServer();
        const client = new ForumClient({
            maxRetries: 0,
            apiKey: "test",
            provisioningKey: "test",
            environment: server.baseUrl,
        });

        const rawResponseBody = { error: { code: "code", message: "message" } };
        server.mockEndpoint().get("/search/").respondWith().statusCode(402).jsonBody(rawResponseBody).build();

        await expect(async () => {
            return await client.search.search();
        }).rejects.toThrow(Forum.PaymentRequiredError);
    });

    test("search (4)", async () => {
        const server = mockServerPool.createServer();
        const client = new ForumClient({
            maxRetries: 0,
            apiKey: "test",
            provisioningKey: "test",
            environment: server.baseUrl,
        });

        const rawResponseBody = { error: { code: "code", message: "message" } };
        server.mockEndpoint().get("/search/").respondWith().statusCode(429).jsonBody(rawResponseBody).build();

        await expect(async () => {
            return await client.search.search();
        }).rejects.toThrow(Forum.TooManyRequestsError);
    });

    test("search (5)", async () => {
        const server = mockServerPool.createServer();
        const client = new ForumClient({
            maxRetries: 0,
            apiKey: "test",
            provisioningKey: "test",
            environment: server.baseUrl,
        });

        const rawResponseBody = { error: { code: "code", message: "message" } };
        server.mockEndpoint().get("/search/").respondWith().statusCode(500).jsonBody(rawResponseBody).build();

        await expect(async () => {
            return await client.search.search();
        }).rejects.toThrow(Forum.InternalServerError);
    });
});
