// This file was auto-generated by Fern from our API Definition.

import { ForumClient } from "../../src/Client";
import { mockServerPool } from "../mock-server/MockServerPool";

describe("PostsClient", () => {
    test("listPosts", async () => {
        const server = mockServerPool.createServer();
        const client = new ForumClient({ maxRetries: 0, token: "test", apiKey: "test", environment: server.baseUrl });

        server.mockEndpoint().get("/posts").respondWith().statusCode(200).build();

        const response = await client.posts.listPosts();
        expect(response).toEqual(undefined);
    });

    test("createPost", async () => {
        const server = mockServerPool.createServer();
        const client = new ForumClient({ maxRetries: 0, token: "test", apiKey: "test", environment: server.baseUrl });
        const rawRequestBody = { key: "value" };

        server.mockEndpoint().post("/posts").jsonBody(rawRequestBody).respondWith().statusCode(200).build();

        const response = await client.posts.createPost({
            key: "value",
        });
        expect(response).toEqual(undefined);
    });

    test("getPost", async () => {
        const server = mockServerPool.createServer();
        const client = new ForumClient({ maxRetries: 0, token: "test", apiKey: "test", environment: server.baseUrl });

        server.mockEndpoint().get("/posts/id").respondWith().statusCode(200).build();

        const response = await client.posts.getPost({
            id: "id",
        });
        expect(response).toEqual(undefined);
    });

    test("deletePost", async () => {
        const server = mockServerPool.createServer();
        const client = new ForumClient({ maxRetries: 0, token: "test", apiKey: "test", environment: server.baseUrl });

        server.mockEndpoint().delete("/posts/id").respondWith().statusCode(200).build();

        const response = await client.posts.deletePost({
            id: "id",
        });
        expect(response).toEqual(undefined);
    });

    test("updatePost", async () => {
        const server = mockServerPool.createServer();
        const client = new ForumClient({ maxRetries: 0, token: "test", apiKey: "test", environment: server.baseUrl });
        const rawRequestBody = { key: "value" };

        server.mockEndpoint().patch("/posts/id").jsonBody(rawRequestBody).respondWith().statusCode(200).build();

        const response = await client.posts.updatePost({
            id: "id",
            body: {
                key: "value",
            },
        });
        expect(response).toEqual(undefined);
    });

    test("listPostReactions", async () => {
        const server = mockServerPool.createServer();
        const client = new ForumClient({ maxRetries: 0, token: "test", apiKey: "test", environment: server.baseUrl });

        server.mockEndpoint().get("/posts/id/reactions").respondWith().statusCode(200).build();

        const response = await client.posts.listPostReactions({
            id: "id",
        });
        expect(response).toEqual(undefined);
    });

    test("addReaction", async () => {
        const server = mockServerPool.createServer();
        const client = new ForumClient({ maxRetries: 0, token: "test", apiKey: "test", environment: server.baseUrl });
        const rawRequestBody = { key: "value" };

        server
            .mockEndpoint()
            .post("/posts/id/reactions")
            .jsonBody(rawRequestBody)
            .respondWith()
            .statusCode(200)
            .build();

        const response = await client.posts.addReaction({
            id: "id",
            body: {
                key: "value",
            },
        });
        expect(response).toEqual(undefined);
    });

    test("removeReaction", async () => {
        const server = mockServerPool.createServer();
        const client = new ForumClient({ maxRetries: 0, token: "test", apiKey: "test", environment: server.baseUrl });

        server.mockEndpoint().delete("/posts/id/reactions").respondWith().statusCode(200).build();

        const response = await client.posts.removeReaction({
            id: "id",
            type: "LIKE",
        });
        expect(response).toEqual(undefined);
    });
});
