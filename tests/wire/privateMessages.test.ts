// This file was auto-generated by Fern from our API Definition.

import { ForumClient } from "../../src/Client";
import { mockServerPool } from "../mock-server/MockServerPool";

describe("PrivateMessagesClient", () => {
    test("listPMs", async () => {
        const server = mockServerPool.createServer();
        const client = new ForumClient({ maxRetries: 0, token: "test", apiKey: "test", environment: server.baseUrl });

        server.mockEndpoint().get("/private-messages").respondWith().statusCode(200).build();

        const response = await client.privateMessages.listPMs();
        expect(response).toEqual(undefined);
    });

    test("sendPm", async () => {
        const server = mockServerPool.createServer();
        const client = new ForumClient({ maxRetries: 0, token: "test", apiKey: "test", environment: server.baseUrl });
        const rawRequestBody = { key: "value" };

        server.mockEndpoint().post("/private-messages").jsonBody(rawRequestBody).respondWith().statusCode(200).build();

        const response = await client.privateMessages.sendPm({
            key: "value",
        });
        expect(response).toEqual(undefined);
    });

    test("getPm", async () => {
        const server = mockServerPool.createServer();
        const client = new ForumClient({ maxRetries: 0, token: "test", apiKey: "test", environment: server.baseUrl });

        server.mockEndpoint().get("/private-messages/id").respondWith().statusCode(200).build();

        const response = await client.privateMessages.getPm({
            id: "id",
        });
        expect(response).toEqual(undefined);
    });

    test("deletePm", async () => {
        const server = mockServerPool.createServer();
        const client = new ForumClient({ maxRetries: 0, token: "test", apiKey: "test", environment: server.baseUrl });

        server.mockEndpoint().delete("/private-messages/id").respondWith().statusCode(200).build();

        const response = await client.privateMessages.deletePm({
            id: "id",
        });
        expect(response).toEqual(undefined);
    });

    test("replyToPm", async () => {
        const server = mockServerPool.createServer();
        const client = new ForumClient({ maxRetries: 0, token: "test", apiKey: "test", environment: server.baseUrl });

        server.mockEndpoint().post("/private-messages/id/replies").respondWith().statusCode(200).build();

        const response = await client.privateMessages.replyToPm({
            id: "id",
        });
        expect(response).toEqual(undefined);
    });
});
