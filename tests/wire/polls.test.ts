// This file was auto-generated by Fern from our API Definition.

import { ForumClient } from "../../src/Client";
import { mockServerPool } from "../mock-server/MockServerPool";

describe("PollsClient", () => {
    test("getPollResults", async () => {
        const server = mockServerPool.createServer();
        const client = new ForumClient({ maxRetries: 0, token: "test", apiKey: "test", environment: server.baseUrl });

        server.mockEndpoint().get("/threads/id/poll").respondWith().statusCode(200).build();

        const response = await client.polls.getPollResults({
            id: "id",
        });
        expect(response).toEqual(undefined);
    });

    test("createPoll", async () => {
        const server = mockServerPool.createServer();
        const client = new ForumClient({ maxRetries: 0, token: "test", apiKey: "test", environment: server.baseUrl });
        const rawRequestBody = { key: "value" };

        server.mockEndpoint().post("/threads/id/poll").jsonBody(rawRequestBody).respondWith().statusCode(200).build();

        const response = await client.polls.createPoll({
            id: "id",
            body: {
                key: "value",
            },
        });
        expect(response).toEqual(undefined);
    });

    test("updateClosePoll", async () => {
        const server = mockServerPool.createServer();
        const client = new ForumClient({ maxRetries: 0, token: "test", apiKey: "test", environment: server.baseUrl });
        const rawRequestBody = { key: "value" };

        server.mockEndpoint().patch("/threads/id/poll").jsonBody(rawRequestBody).respondWith().statusCode(200).build();

        const response = await client.polls.updateClosePoll({
            id: "id",
            body: {
                key: "value",
            },
        });
        expect(response).toEqual(undefined);
    });

    test("castVote", async () => {
        const server = mockServerPool.createServer();
        const client = new ForumClient({ maxRetries: 0, token: "test", apiKey: "test", environment: server.baseUrl });
        const rawRequestBody = { key: "value" };

        server
            .mockEndpoint()
            .post("/threads/id/poll/votes")
            .jsonBody(rawRequestBody)
            .respondWith()
            .statusCode(200)
            .build();

        const response = await client.polls.castVote({
            id: "id",
            body: {
                key: "value",
            },
        });
        expect(response).toEqual(undefined);
    });

    test("removeVote", async () => {
        const server = mockServerPool.createServer();
        const client = new ForumClient({ maxRetries: 0, token: "test", apiKey: "test", environment: server.baseUrl });

        server.mockEndpoint().delete("/threads/id/poll/votes").respondWith().statusCode(200).build();

        const response = await client.polls.removeVote({
            id: "id",
        });
        expect(response).toEqual(undefined);
    });
});
